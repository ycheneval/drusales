#
# Apache/PHP/Drupal settings:
#
# Protect files and directories from prying eyes. Various rewrite rules.
<IfModule mod_rewrite.c>
  RewriteEngine on
  RewriteRule ^(.*)$ http://www.weforum.org/pdf/$1 [R=301,L]
</IfModule>
# $Id$

